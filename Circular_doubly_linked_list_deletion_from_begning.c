// CIRCULAR DOUBLY LINKED LIST DELETION FROM BEGNING
//#include<stdio.h>
//while(tail->next!=head){
//	tail=tail->next;
//}
//head=head->next;
//head->prev=tail;
//tail->next=head;
//free(temp);
