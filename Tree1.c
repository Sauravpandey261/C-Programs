#include<stdio.h>
struct node{
	int data;
	struct node *left, *right;
};
struct node *create(){
	int data;
	newnode = (struct node *)malloc(sizeof(struct node));
	printf("Press 0 to Exit:")
}
